export const selectStats = (s) => s.landing.stats;
export const selectPopular = (s) => s.landing.popular;
export const selectTestimonials = (s) => s.landing.testimonials;
export const selectFaq = (s) => s.landing.faq;
